<resources xmlns:android="http://schemas.android.com/apk/res/android">
	<!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Black.NoTitleBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>
    
    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        
        <item name="android:windowAnimationStyle">@style/CustomAnimationLTR</item>
    </style>

    <style name="AppThemeRTL" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        
        <item name="android:windowAnimationStyle">@style/CustomAnimationRTL</item>
    </style>
    
    
    <style name="ViewBasics" parent="AppTheme">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ViewBasicsForWeight" parent="AppTheme">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ViewBasicsBoldItalic" parent="AppTheme">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold|italic</item>
    </style>

    <style name="CustomAnimationLTR">
        <item name="android:activityOpenEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:activityOpenExitAnimation">@android:anim/slide_out_right</item>
<!--         <item name="android:activityCloseEnterAnimation">@android:anim/slide_in_left</item> -->
        <item name="android:activityCloseExitAnimation">@android:anim/slide_out_right</item>
    </style>
    
        <style name="CustomAnimationRTL">
        <item name="android:activityOpenEnterAnimation">@android:anim/slide_out_right</item>
        <item name="android:activityOpenExitAnimation">@android:anim/slide_in_left</item>
<!--         <item name="android:activityCloseEnterAnimation">@android:anim/slide_in_left</item> -->
        <item name="android:activityCloseExitAnimation">@android:anim/slide_in_left</item>
    </style>

    
    
</resources>